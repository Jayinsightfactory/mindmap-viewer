-- Initial schema (already created by createTables, this is a no-op marker)
-- All tables are created by src/db-pg.js createTables()
-- This migration exists to establish the migration tracking baseline
SELECT 1;
